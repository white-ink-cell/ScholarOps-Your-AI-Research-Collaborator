# Expected gates

- empirical entry/state gate;
- Seen Data/blindness gate;
- Freeze A / screen progression criteria;
- `GATE-SCREEN-REUSE-POLICY` if the screen data may later enter formal estimation;
- within-study multiplicity only if the screen actually creates multiple confirmatory claims.

The formal economic-threshold gate is **not automatically active** merely because the progression criterion has economic units.
