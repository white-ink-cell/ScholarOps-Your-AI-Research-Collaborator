# Synthetic input

A research team is evaluating a new workflow that automatically extracts covenant clauses from corporate filings before human review.

They have already validated extraction accuracy without opening the research outcome. They now want a small `OUTCOME_DECISION_SCREEN` to decide whether it is worth funding a larger formal study of analyst review efficiency.

Before seeing the screen outcome they propose this progression criterion:

> Continue only if the screen estimates that the workflow saves at least 8 minutes per reviewed filing, measured using the frozen screen sample and estimator.

They also want to write, if the screen fails:

> “The workflow has no economically meaningful productivity effect.”

Task: Apply ScholarOps to the screen design and interpretation.
