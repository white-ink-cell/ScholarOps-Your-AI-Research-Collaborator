# Expected gates

- `GATE-CONSTRUCT-IDENTITY` — active.
- `GATE-SOURCE-QUALITY` — active.
- `GATE-STATE-NOSKIP` — active at the workflow entry boundary.
- L4 evidence-sufficiency logic (`CORE-L4-04` / `CORE-L4-10`) — active.

`Paper B` is a dangerous verification lead, but the unavailable method details mean it cannot by itself support formal narrowing if the adverse proposition requires those details.

`Paper C` is sufficiently direct and high-quality to enter formal contribution/collision adjudication.
